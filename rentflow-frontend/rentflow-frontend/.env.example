# Base URL of the Express + Prisma backend
VITE_API_BASE_URL=http://localhost:4000/api

# true  -> app runs standalone against in-memory mock data (src/services/mockData.ts)
# false -> every service call goes to VITE_API_BASE_URL instead
VITE_USE_MOCKS=true
